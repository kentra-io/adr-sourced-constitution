---
name: plan-gate
description: Reads a plan plus the active constitution.md, reasons rule-by-rule, runs `constitution guard`, and emits deviation.json citing the ADR ids a plan would violate. Invoke explicitly with /plan-gate before executing a plan.
disable-model-invocation: true
---

# plan-gate (stub)

Placeholder skill body. M4 ships this stub with valid frontmatter and the
correct invocation stance only; the real rule-by-rule gate logic (plan §6) is
authored in M5. Do not rely on this body for behavior yet.
